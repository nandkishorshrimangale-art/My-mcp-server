# My-mcp-server
Simple MCP Server is a Python-based project that implements the Model Context Protocol (MCP). It provides tools for calculations and user interactions, enabling AI assistants to access external functions through a standardized interface. The project is lightweight, easy to extend, and demonstrates MCP client-server communication effectively.
